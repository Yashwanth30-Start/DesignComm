# database

V1 placeholder. No database is connected yet — `lib/mock-data.ts` stands in for it. When a real backend lands, its client/queries go here, targeting the schema already drafted in `../../backend/schema.sql`.
