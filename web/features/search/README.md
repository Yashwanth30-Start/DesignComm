# features/search

V1 placeholder. Global AI search — the single command bar that answers "what does this asset/panel/circuit/RFI touch" — builds here against the normalized record contract in `../../docs/integration-contract.md`. The `Topbar` component already reserves the search bar's visual slot.
