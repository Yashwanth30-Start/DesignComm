# features/lifesafety

V1 placeholder. Fire alarm, fire/smoke dampers, and rapid shutter/overhead door tracking views build here, sharing the same `Asset` type and `StatusPill`/`Timeline` components as every other asset class.
