# features/workflow

V1 placeholder. This is where the workflow engine lives: the logic that derives Ready / Waiting / Blocked / Commissioned / Complete from linked records instead of asking the user to interpret trackers by hand, plus the homepage's "Live Workflow Queue." Builds next, after the design system pass.
